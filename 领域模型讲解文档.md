# 快递物流管理系统领域模型讲解文档

## 1. 概述

本文档详细讲解快递物流管理系统的领域模型设计。该模型采用**领域驱动设计（DDD）**的思想，将整个系统划分为多个**有界上下文（Bounded Context）**，每个上下文代表一个独立的业务领域。

### 1.1 设计原则

- **聚合根（Aggregate Root）**：每个聚合都有一个聚合根，负责维护聚合内的业务一致性
- **实体（Entity）**：具有唯一标识的业务对象
- **值对象（Value Object）**：没有唯一标识，通过属性值来区分
- **领域服务（Domain Service）**：处理跨聚合的业务逻辑
- **仓储（Repository）**：封装数据访问逻辑

### 1.2 模型结构

系统共划分为**6个核心领域**：
1. 包裹管理领域（Package Management Domain）
2. 运输调度领域（Transport Scheduling Domain）
3. 派送领域（Delivery Domain）
4. 异常处理领域（Exception Handling Domain）
5. 客户管理领域（Customer Management Domain）
6. 网点与站点领域（Site and Station Domain）

---

## 2. 包裹管理领域（Package Management Domain）

### 2.1 领域概述

包裹管理领域是系统的**核心领域**，包裹（Package）作为整个业务流程的载体，贯穿从揽收到签收的完整生命周期。

### 2.2 核心实体

#### 2.2.1 Package（包裹）- 聚合根

**职责**：作为包裹管理聚合的根实体，管理包裹的完整生命周期。

**关键属性**：
- `packageId`: 包裹唯一标识
- `waybillNumber`: 运单号（业务唯一标识）
- `weight`, `length`, `width`, `height`: 包裹物理属性
- `currentStatus`: 当前状态（PackageStatus枚举）
- `currentLocation`: 当前位置（Location值对象）
- `estimatedArrivalTime`: 预计到达时间

**核心方法**：
- `updateStatus(status)`: 更新包裹状态，触发状态机转换
- `addTrace(trace)`: 添加轨迹记录，记录包裹流转历史
- `getTraceHistory()`: 获取完整轨迹历史
- `markAsException(reason)`: 标记为异常状态
- `getCurrentLocation()`: 获取当前位置信息
- `getNextStation()`: 确定下一站站点

**设计要点**：
- Package是聚合根，负责维护包裹的业务一致性
- 通过Waybill关联寄件人和收件人信息
- 通过PackageTrace记录完整的流转轨迹

#### 2.2.2 Waybill（运单）

**职责**：记录包裹的寄件和收件信息，是包裹的业务凭证。

**关键属性**：
- `waybillNumber`: 运单号（唯一）
- `senderInfo`: 寄件人信息（Customer）
- `recipientInfo`: 收件人信息（Customer）
- `originSite`: 始发网点
- `destinationSite`: 目的网点
- `declaredValue`: 声明价值
- `createdTime`: 创建时间

**核心方法**：
- `validate()`: 验证运单信息完整性
- `print()`: 打印运单
- `getSenderAddress()`: 获取寄件地址
- `getRecipientAddress()`: 获取收件地址

**设计要点**：
- Waybill与Package是一对一关系
- 运单号是业务主键，用于客户查询

#### 2.2.3 PackageTrace（包裹轨迹）

**职责**：记录包裹在流转过程中的每个状态变化和位置信息。

**关键属性**：
- `traceId`: 轨迹记录ID
- `packageId`: 关联的包裹ID
- `location`: 位置信息（Location值对象）
- `status`: 状态（PackageStatus枚举）
- `timestamp`: 时间戳
- `operator`: 操作人
- `operatorType`: 操作人类型
- `description`: 描述信息

**核心方法**：
- `getLocationName()`: 获取位置名称
- `getStatusDescription()`: 获取状态描述

**设计要点**：
- 每个状态变化都会生成一条轨迹记录
- 轨迹记录不可修改，保证历史记录的完整性

### 2.3 枚举类型

#### PackageStatus（包裹状态）

定义了包裹的完整生命周期状态：

```
COLLECTED          - 已揽收：包裹已被快递员收取
SORTING            - 分拣中：包裹在分拣中心进行分拣
IN_TRANSIT         - 运输中：包裹正在运输途中
ARRIVED_AT_TRANSFER - 到达中转站：包裹到达中转站
OUT_FOR_DELIVERY   - 派送中：包裹正在派送途中
DELIVERED          - 已签收：包裹已被收件人签收
EXCEPTION          - 异常：包裹出现异常情况
RETURNED           - 已退回：包裹被退回
```

**状态转换规则**：
- 状态转换必须符合业务规则
- 异常状态需要记录异常原因
- 已签收状态是终态，不能再转换

---

## 3. 运输调度领域（Transport Scheduling Domain）

### 3.1 领域概述

运输调度领域负责管理包裹在网点之间的运输过程，包括路径规划、车辆调度、司机分配等核心业务。

### 3.2 核心实体

#### 3.2.1 TransportTask（运输任务）- 聚合根

**职责**：作为运输调度聚合的根实体，管理一次完整的运输任务。

**关键属性**：
- `taskId`: 任务唯一标识
- `packageList`: 包裹列表（本次运输的包裹ID列表）
- `packageCount`: 包裹数量
- `originStationId`: 始发站点ID
- `destinationStationId`: 目的站点ID
- `assignedVehicle`: 分配的车辆
- `assignedDriver`: 分配的司机
- `plannedRoute`: 规划的路线
- `taskStatus`: 任务状态（TaskStatus枚举）
- `scheduledTime`: 计划出发时间
- `actualDepartureTime`: 实际出发时间
- `actualArrivalTime`: 实际到达时间

**核心方法**：
- `assignVehicleAndDriver(vehicle, driver)`: 分配车辆和司机
- `addPackage(packageId)`: 添加包裹到任务
- `startTransport()`: 开始运输
- `completeTransport()`: 完成运输
- `handleRouteChange(newRoute)`: 处理路线变更
- `getLoadRate()`: 计算装载率

**设计要点**：
- TransportTask是聚合根，负责维护运输任务的业务一致性
- 通过关联Vehicle和Driver实现资源调度
- 支持路线变更等扩展流程

#### 3.2.2 Vehicle（车辆）

**职责**：管理车辆信息和状态。

**关键属性**：
- `vehicleId`: 车辆唯一标识
- `vehicleNumber`: 车牌号
- `vehicleType`: 车辆类型（VehicleType枚举）
- `capacity`: 载重能力
- `maxVolume`: 最大容积
- `currentStatus`: 当前状态（VehicleStatus枚举）
- `maintenanceDate`: 维护日期

**核心方法**：
- `checkAvailability()`: 检查车辆是否可用
- `calculateLoadRate(packages)`: 计算装载率
- `updateStatus(status)`: 更新车辆状态

**设计要点**：
- 车辆状态影响任务分配
- 需要检查车辆可用性才能分配任务

#### 3.2.3 Driver（司机）

**职责**：管理司机信息和任务状态。

**关键属性**：
- `driverId`: 司机唯一标识
- `name`: 姓名
- `phoneNumber`: 联系电话
- `licenseNumber`: 驾驶证号
- `licenseType`: 驾驶证类型
- `currentStatus`: 当前状态（DriverStatus枚举）
- `currentTask`: 当前任务ID

**核心方法**：
- `viewTransportTask()`: 查看运输任务列表
- `startTask(taskId)`: 开始任务
- `completeTask(taskId)`: 完成任务
- `reportRouteChange(reason)`: 报告路线变更
- `updateStatus(status)`: 更新司机状态

**设计要点**：
- 司机状态影响任务分配
- 支持司机查看和管理自己的任务

#### 3.2.4 RoutePlan（路线规划）

**职责**：管理运输路线的规划信息。

**关键属性**：
- `routeId`: 路线唯一标识
- `originLocationId`: 起点位置ID
- `destinationLocationId`: 终点位置ID
- `intermediateStationIds`: 中转站点ID列表
- `estimatedTime`: 预计时间
- `distance`: 距离
- `planType`: 规划类型（PlanType枚举）

**核心方法**：
- `calculateRoute()`: 计算最优路线
- `updateRoute(newStationIds)`: 更新路线
- `getNextStationId(currentStationId)`: 获取下一站
- `estimateArrivalTime(currentStationId)`: 估算到达时间

**设计要点**：
- 支持多种规划策略（最短路径、最快路径、成本最优）
- 路线可以动态调整

### 3.3 枚举类型

#### TaskStatus（任务状态）
```
PENDING      - 待处理：任务已创建，等待分配资源
ASSIGNED     - 已分配：已分配车辆和司机
IN_PROGRESS  - 进行中：运输任务正在进行
COMPLETED    - 已完成：运输任务已完成
CANCELLED    - 已取消：任务被取消
```

#### VehicleType（车辆类型）
```
SMALL_VAN        - 小型厢式车
MEDIUM_TRUCK     - 中型卡车
LARGE_TRUCK      - 大型卡车
ELECTRIC_VEHICLE - 电动车
```

#### VehicleStatus（车辆状态）
```
AVAILABLE   - 可用：车辆可用，可以分配任务
IN_USE      - 使用中：车辆正在执行任务
MAINTENANCE - 维护中：车辆正在维护
BREAKDOWN   - 故障：车辆出现故障
```

#### DriverStatus（司机状态）
```
AVAILABLE - 可用：司机可用，可以分配任务
ON_DUTY   - 执勤中：司机正在执勤
OFF_DUTY  - 休息中：司机休息中
ON_LEAVE  - 请假：司机请假中
```

#### PlanType（计划类型）
```
SHORTEST_PATH  - 最短路径：距离最短
FASTEST_PATH   - 最快路径：时间最短
COST_OPTIMAL   - 成本最优：成本最低
```

---

## 4. 派送领域（Delivery Domain）

### 4.1 领域概述

派送领域负责管理包裹从目的网点到收件人的最后一段配送过程，包括派送任务分配、签收确认等业务。

### 4.2 核心实体

#### 4.2.1 DeliveryTask（派送任务）- 聚合根

**职责**：作为派送聚合的根实体，管理一次派送任务的完整过程。

**关键属性**：
- `taskId`: 任务唯一标识
- `packageList`: 包裹列表
- `assignedCourier`: 分配的派送员
- `deliveryArea`: 派送区域
- `taskStatus`: 任务状态（TaskStatus枚举）
- `scheduledDate`: 计划派送日期
- `completedCount`: 已完成数量
- `totalCount`: 总数量

**核心方法**：
- `assignCourier(courier)`: 分配派送员
- `addPackage(packageId)`: 添加包裹
- `optimizeDeliveryRoute()`: 优化派送路线
- `completeDelivery(packageId)`: 完成单个包裹派送
- `getProgress()`: 获取任务进度

**设计要点**：
- DeliveryTask是聚合根，管理派送任务的业务一致性
- 支持批量派送，一个任务可以包含多个包裹
- 跟踪任务进度，支持部分完成

#### 4.2.2 Courier（快递员）

**职责**：管理派送员信息和任务状态。

**关键属性**：
- `courierId`: 派送员唯一标识
- `name`: 姓名
- `phoneNumber`: 联系电话
- `assignedArea`: 负责区域
- `currentStatus`: 当前状态（CourierStatus枚举）
- `vehicleType`: 交通工具类型
- `currentTask`: 当前任务ID

**核心方法**：
- `viewDeliveryTasks()`: 查看派送任务列表
- `deliverPackage(packageId)`: 派送包裹
- `confirmSignature(packageId, signature)`: 确认签收
- `reportException(packageId, reason)`: 报告异常
- `updateStatus(status)`: 更新状态

**设计要点**：
- 派送员可以查看和管理自己的任务
- 支持异常报告功能

#### 4.2.3 SignatureRecord（签收记录）

**职责**：记录包裹的签收信息，作为签收凭证。

**关键属性**：
- `recordId`: 记录唯一标识
- `packageId`: 包裹ID
- `waybillNumber`: 运单号
- `signatureTime`: 签收时间
- `signaturePerson`: 签收人
- `relationToRecipient`: 与收件人关系
- `signatureImage`: 签收图片
- `courierId`: 派送员ID
- `deliveryTaskId`: 派送任务ID

**核心方法**：
- `validate()`: 验证签收信息完整性
- `getImageUrl()`: 获取签收图片URL

**设计要点**：
- 签收记录是重要的业务凭证
- 支持签收图片存储

### 4.3 枚举类型

#### CourierStatus（快递员状态）
```
AVAILABLE  - 可用：派送员可用，可以分配任务
DELIVERING - 派送中：正在派送包裹
RETURNING  - 返回中：正在返回网点
OFF_DUTY   - 休息中：派送员休息中
```

---

## 5. 异常处理领域（Exception Handling Domain）

### 5.1 领域概述

异常处理领域负责管理包裹流转过程中出现的各种异常情况，包括异常报告、处理分配、解决方案等。

### 5.2 核心实体

#### 5.2.1 ExceptionRecord（异常记录）

**职责**：记录和管理包裹的异常情况。

**关键属性**：
- `exceptionId`: 异常记录唯一标识
- `packageId`: 关联的包裹ID
- `exceptionType`: 异常类型（ExceptionType枚举）
- `exceptionReason`: 异常原因
- `reportTime`: 报告时间
- `reportPerson`: 报告人
- `reportPersonRole`: 报告人角色
- `handlingStatus`: 处理状态（HandlingStatus枚举）
- `assignedHandler`: 分配的处理人
- `resolutionPlan`: 解决方案
- `resolvedTime`: 解决时间
- `resolutionNote`: 解决备注

**核心方法**：
- `createException()`: 创建异常记录
- `assignHandler(handlerId)`: 分配处理人
- `resolveException(solution)`: 解决异常
- `notifyCustomer(message)`: 通知客户
- `escalate()`: 升级异常

**设计要点**：
- 异常记录贯穿异常处理的完整生命周期
- 支持异常升级机制
- 需要通知客户异常情况

### 5.3 枚举类型

#### ExceptionType（异常类型）
```
SORTING_EXCEPTION      - 分拣异常：分拣过程中出现问题
TRANSPORT_EXCEPTION    - 运输异常：运输过程中出现问题
DELIVERY_EXCEPTION     - 派送异常：派送过程中出现问题
PACKAGE_DAMAGE         - 包裹破损：包裹在流转中破损
ADDRESS_ERROR          - 地址错误：收件地址不正确
RECIPIENT_UNAVAILABLE  - 收件人不在：收件人不在指定地址
WEATHER_DELAY          - 天气延误：因天气原因延误
TRAFFIC_DELAY          - 交通延误：因交通原因延误
OTHER                  - 其他：其他异常情况
```

#### HandlingStatus（处理状态）
```
REPORTED   - 已报告：异常已报告，等待处理
ASSIGNED   - 已分配：已分配给处理人
IN_PROGRESS - 处理中：正在处理异常
RESOLVED   - 已解决：异常已解决
ESCALATED  - 已升级：异常已升级到上级处理
```

---

## 6. 客户管理领域（Customer Management Domain）

### 6.1 领域概述

客户管理领域负责管理寄件人和收件人的信息，支持客户创建运单、查询包裹状态等功能。

### 6.2 核心实体

#### 6.2.1 Customer（客户）

**职责**：管理客户信息。

**关键属性**：
- `customerId`: 客户唯一标识
- `name`: 姓名
- `phoneNumber`: 联系电话
- `address`: 地址（Address值对象）
- `customerType`: 客户类型（CustomerType枚举）

**核心方法**：
- `createWaybill(recipient, packageInfo)`: 创建运单
- `queryPackageStatus(waybillNumber)`: 查询包裹状态
- `updateContactInfo(phone, address)`: 更新联系信息

**设计要点**：
- 客户可以是寄件人、收件人或两者
- 支持客户自主创建运单和查询

### 6.3 枚举类型

#### CustomerType（客户类型）
```
SENDER   - 寄件人：仅作为寄件人
RECIPIENT - 收件人：仅作为收件人
BOTH     - 两者：既是寄件人也是收件人
```

---

## 7. 网点与站点领域（Site and Station Domain）

### 7.1 领域概述

网点与站点领域负责管理物流网络中的各个节点，包括服务网点、分拣中心、中转站等。

### 7.2 核心实体

#### 7.2.1 Station（站点）- 抽象基类

**职责**：作为所有站点的基类，定义站点的通用属性和行为。

**关键属性**：
- `stationId`: 站点唯一标识
- `stationName`: 站点名称
- `location`: 位置信息（Location值对象）
- `capacity`: 容量
- `currentLoad`: 当前负载

**核心方法**：
- `acceptPackage(package)`: 接收包裹
- `checkCapacity()`: 检查容量

**设计要点**：
- Station是抽象类，使用继承实现不同类型的站点
- 通过Location值对象管理位置信息

#### 7.2.2 Site（网点）

**职责**：管理服务网点，负责包裹的揽收和派送。

**关键属性**：
- `siteType`: 网点类型（SiteType枚举）
- `operatingHours`: 营业时间
- `contactPhone`: 联系电话

**核心方法**：
- `acceptPackage(package)`: 接收包裹
- `sortPackage(package)`: 分拣包裹
- `assignDeliveryTask(packages)`: 分配派送任务
- `printWaybill(waybill)`: 打印运单

**设计要点**：
- Site继承自Station
- 支持不同类型的网点（服务网点、分拣中心、仓库）

#### 7.2.3 TransferStation（中转站）

**职责**：管理中转站，负责包裹的分拣和转运。

**关键属性**：
- `sortingCapacity`: 分拣能力
- `sortingRules`: 分拣规则（SortingRule值对象）
- `connectedStationIds`: 连接的站点ID列表

**核心方法**：
- `sortPackages(packages)`: 批量分拣包裹
- `generateTransportTasks()`: 生成运输任务
- `applySortingRule(package)`: 应用分拣规则确定下一站

**设计要点**：
- TransferStation继承自Station
- 通过分拣规则自动确定包裹的下一站
- 支持批量分拣和任务生成

### 7.3 枚举类型

#### SiteType（网点类型）
```
SERVICE_SITE   - 服务网点：面向客户的服务网点
SORTING_CENTER - 分拣中心：专门进行包裹分拣的中心
WAREHOUSE      - 仓库：存储包裹的仓库
```

---

## 8. 值对象（Value Objects）

### 8.1 Location（位置）

**职责**：表示一个地理位置信息。

**关键属性**：
- `locationId`: 位置ID
- `locationType`: 位置类型（LocationType枚举）
- `locationName`: 位置名称
- `address`: 地址信息（Address值对象）
- `coordinates`: 坐标信息（Coordinates值对象）

**核心方法**：
- `getFullName()`: 获取完整位置名称
- `distanceTo(other)`: 计算到另一个位置的距离

**设计要点**：
- Location是不可变的值对象
- 通过地址和坐标双重定位

### 8.2 Address（地址）

**职责**：表示一个地址信息。

**关键属性**：
- `province`: 省
- `city`: 市
- `district`: 区
- `street`: 街道
- `detailAddress`: 详细地址
- `postalCode`: 邮编

**核心方法**：
- `validateAddress()`: 验证地址有效性
- `getFullAddress()`: 获取完整地址字符串
- `isSameCity(other)`: 判断是否同城

**设计要点**：
- Address是不可变的值对象
- 支持地址验证和格式化

### 8.3 Coordinates（坐标）

**职责**：表示地理坐标信息。

**关键属性**：
- `longitude`: 经度
- `latitude`: 纬度

**设计要点**：
- Coordinates是不可变的值对象
- 用于计算距离和位置定位

### 8.4 SortingRule（分拣规则）

**职责**：定义包裹分拣的规则。

**关键属性**：
- `ruleId`: 规则ID
- `ruleName`: 规则名称
- `priority`: 优先级

**核心方法**：
- `determineNextStation(package)`: 确定下一站
- `validate()`: 验证规则有效性

**设计要点**：
- SortingRule是值对象，封装分拣逻辑
- 支持规则优先级

### 8.5 枚举类型

#### LocationType（位置类型）
```
SITE            - 网点：服务网点位置
TRANSFER_STATION - 中转站：中转站位置
VEHICLE         - 车辆：车辆位置
OTHER           - 其他：其他位置类型
```

---

## 9. 领域间关系

### 9.1 包裹管理与运输调度的关系

- **Package** 通过 `packageId` 被 **TransportTask** 引用
- TransportTask管理多个Package的运输
- Package的状态变化会触发TransportTask的状态更新

### 9.2 包裹管理与派送领域的关系

- **Package** 通过 `packageId` 被 **DeliveryTask** 引用
- DeliveryTask管理Package的最后一段配送
- Package的签收会更新DeliveryTask的进度

### 9.3 异常处理与其他领域的关系

- **ExceptionRecord** 通过 `packageId` 关联到 **Package**
- 异常可以发生在分拣、运输、派送等各个环节
- 异常处理会影响Package的状态

### 9.4 客户管理与包裹管理的关系

- **Customer** 通过 **Waybill** 关联到 **Package**
- Waybill记录寄件人和收件人信息
- Customer可以查询Package的状态

### 9.5 站点与其他领域的关系

- **Station**（Site/TransferStation）作为包裹流转的节点
- TransportTask的起点和终点都是Station
- Package的当前位置是Station

---

## 10. 设计模式应用

### 10.1 聚合模式

- **Package聚合**：Package作为聚合根，管理Waybill和PackageTrace
- **TransportTask聚合**：TransportTask作为聚合根，管理Vehicle和Driver的分配
- **DeliveryTask聚合**：DeliveryTask作为聚合根，管理Courier的分配

### 10.2 值对象模式

- Location、Address、Coordinates、SortingRule都是值对象
- 值对象不可变，通过属性值来区分
- 值对象可以嵌入到实体中

### 10.3 仓储模式

- 每个聚合根都有对应的Repository
- Repository封装数据访问逻辑
- Repository提供领域友好的查询接口

### 10.4 领域服务模式

- 跨聚合的业务逻辑放在领域服务中
- 如路径规划、异常处理等复杂业务逻辑

---

## 11. 业务流程支持

### 11.1 主流程

1. **揽收**：Customer创建Waybill，Package状态变为COLLECTED
2. **分拣**：Site分拣包裹，Package状态变为SORTING
3. **路径规划**：系统规划运输路线，创建RoutePlan
4. **运输任务**：创建TransportTask，分配Vehicle和Driver
5. **运输执行**：Driver开始运输，Package状态变为IN_TRANSIT
6. **到达中转站**：Package到达中转站，状态变为ARRIVED_AT_TRANSFER
7. **派送任务**：创建DeliveryTask，分配Courier
8. **派送执行**：Courier派送包裹，Package状态变为OUT_FOR_DELIVERY
9. **签收确认**：收件人签收，Package状态变为DELIVERED

### 11.2 扩展流程

1. **路线变更**：TransportTask支持路线变更
2. **异常处理**：任何环节都可以报告异常，创建ExceptionRecord
3. **包裹退回**：异常情况下，Package可以退回，状态变为RETURNED

### 11.3 基本能力

1. **包裹状态查询**：通过waybillNumber查询Package状态
2. **轨迹查询**：查询Package的完整轨迹历史
3. **任务管理**：Driver和Courier可以查看自己的任务
4. **异常处理**：支持异常报告、分配、解决等完整流程

---

## 12. 总结

本领域模型设计遵循DDD原则，通过以下方式实现了良好的领域建模：

1. **清晰的边界**：通过有界上下文划分不同的业务领域
2. **聚合设计**：每个聚合都有明确的聚合根，维护业务一致性
3. **值对象使用**：合理使用值对象，提高模型的表达力
4. **枚举类型**：使用枚举类型表达领域概念，提高代码可读性
5. **关系设计**：通过ID引用而非直接对象引用，降低耦合度

该模型能够很好地支持快递物流管理系统的核心业务流程，同时具备良好的扩展性，可以支持未来的业务需求变化。

